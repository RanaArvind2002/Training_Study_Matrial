//4.	Write a program to check weather divisible by 5 or not?
#include<stdio.h>
int main(){
int num;
printf("Enter the Number :- ");
scanf("%d",&num);
if(num%5==0)
{
 printf("The Number are Divisible by 5.");
}
else
{
 printf(" The Number are not Divisible by 5.");
}
}
